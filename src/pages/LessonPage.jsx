/**
 * Lesson UI lives in `components/platform/LessonModule.jsx` and is composed inside
 * `app/dataspark-full-platform.jsx` for the /platform route. Re-export for backlog / imports.
 */
export { default } from "../components/platform/LessonModule.jsx";
